# SG-L8 Webを狙う攻撃①：入力をだます（sg-L8-injection-attacks）

発注書: `content_works/ipa_sg/orders/L8.md`（攻撃側だけを扱う。対策＝プレースホルダ・エスケープは L46、
利用者を狙う攻撃は L9、HTTP・cookie の詳細は L63）。

- 一言で: 入力欄に「データのつもりでない文字」を入れられると、サーバが命令として実行してしまう
- 総ページ 14（title→前提→概念→SQL→被害→OSコマンド→トラバーサル→BOF→共通原因→幕間→クイズ3→まとめ）
- 原稿の総文字数 約1,270字（共通音声のクイズ導入・締めを除く）

## 読みの例外（音声と字幕を分けている箇所）

`SQL` は TTS が誤読しうるため、**音声用テキストだけ「エスキューエル」と仮名書き**にし、
字幕（`N()` の第2引数）は `SQL` の表記のままにしている（references/narration.md の例外規定）。
対象は s04-1 / s04-2 / s05-1 / s11-2。**原稿と音声が食い違っているように見えるが意図的。**

## ページ表

| # | メッセージ | あるべき姿 | 形（レイアウト） |
|---|---|---|---|
| 1 | title | — | title（ジングルのみ・ナレーションなし） |
| 2 | 入力はサーバのプログラムを通ってデータベースへ届く | 一直線のパイプライン | custom: ブラウザ→サーバ→DB の3ノード（既存イラスト icon-browser / icon-server / icon-database）＋矢印 |
| 3 | データのつもりの文字が「命令」になる＝インジェクション | 短い主張＋絵 | custom: 左右分割（主張＋ attack-injection.png） |
| 4 | SQLインジェクションは命令文に入力がはめ込まれて起きる | コードが書き換わる様子 | custom: SQL文テンプレート＋正常入力／攻撃入力の2行（等幅） |
| 5 | 被害は漏えい・改ざん・認証回避の3つ | 並列3項目 | bullets（appearAtSec で1文ずつ同期） |
| 6 | 同じ理屈が OS への命令でも起きる | SQL との対比 | vs（columnAtSec で同期）＋ wipe-light |
| 7 | パスをさかのぼって非公開ファイルを読まれる | 階層と上向きの経路 | custom: ディレクトリツリー＋さかのぼる矢印 |
| 8 | 想定より長い入力が隣の領域を上書きする | 箱からあふれる帯 | custom: 確保領域／隣接領域のバーと侵入 |
| 9 | 4つの原因はすべて「入力を信用した」こと | 4つが1つに収束 | custom: 攻撃名チップ4つ→結論カード（対策は L46 へ渡す） |
| 10 | クイズ導入 | — | SectionTitle「クイズで確認」＋共通音声 QUIZ_INTRO_SEG |
| 11 | SQLiで起きること | — | quiz（正解A） |
| 12 | さかのぼって読む攻撃 | — | quiz（正解B） |
| 13 | 共通する原因 | — | quiz（正解A） |
| 14 | まとめ | チェックリスト | summary（transitionIn: wipe）＋ OUTRO_SEG |

正解位置は A / B / A（片寄り防止）。

## 原稿

原稿の正は `sg-L8-injection-attacks.jobs.json`（音声）と `src/videos/ipa_sg/sg-L8-injection-attacks.tsx` の
`N()` 第2引数（字幕）。上記の読みの例外を除いて両者は同一。

## 用語の呼称（L2 の表に揃える）

- 攻撃者が突く弱点は「脆弱性」、実際に被害を起こす行為は「攻撃」と呼ぶ
- この回では対策を語らない（「〜すれば防げる」と言い切らず、L46 へ渡す）
