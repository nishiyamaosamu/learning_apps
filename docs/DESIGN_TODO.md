# DESIGN_TODO（未実装メモ）

`docs/DESIGN.html`「集中ブルー v1.1」のうち、engine への適用にあたって**意図的に未実装のまま見送った**機能・導線の一覧。
見た目のトークン・コンポーネント（色/タイポ/角丸/シャドウ/クイズキット等）は適用済みだが、ここに挙げる項目はモック上の表現のみが先行しており、裏側のロジック（永続化・集計・再生等）は実装していない。

**実装時はその項目をこのファイルから削除すること。**

---

## 4. ストリーク（連続学習日数）チップ

- **何を作るか**: 学習した日をカウントし、連続日数をアプリバーに表示する。
- **DESIGN.html 上の対応**: アプリバー右上の `chip-review` チップ（アンバー、例:「12日連続」）／クイズ完了statsの「連続日数」表示。
- **依存するもの**: 日次の学習実施記録を保存する永続化レイヤー（日付単位のログ）。

## 5. プレミアム導線

- **何を作るか**: 課金プラン（広告なし・全年度問題集・AIに質問し放題等）の訴求UIと、実際の購入/トライアル導線。
- **DESIGN.html 上の対応**: アプリバーの王冠アイコン（`prem`、`workspace_premium`、primary100面+primary800）／設定タブ先頭の「絶対合格プレミアム」ヒーローカード（primary600面＋円装飾＋「7日間無料でためす」CTA）。
- **依存するもの**: 課金基盤（in_app_purchase 等）、購入状態の永続化。**ルール4に従い、実装時も課金導線の配色は review系（アンバー）ではなく primary 系で組むこと**（アンバーは要復習・ストリーク専用）。

## 6. 「AIに質問」ボタン

- **何を作るか**: 動画視聴ページから質問できるAI機能（実際の質問応答・API連携）。
- **DESIGN.html 上の対応**: 動画視聴ページの `btn-ai`（surface面＋枠・文字にprimary600→`--aiViolet`(#7C3AED)グラデーションを通す、`auto_awesome`アイコン＋「AIに質問」）。ボタンの見た目自体は実装対象に含めてよいが、タップ後の質問応答フローは未実装。
- **依存するもの**: LLM API連携（バックエンド or クライアント直叩き）、質問UI（入力欄・回答表示画面）。

## 8. 設定タブの残り項目

- **何を作るか**: リマインダー設定（通知時刻の設定・ローカル通知のスケジューリング）、サポート節の「お問い合わせ」「利用規約・プライバシー」（外部リンク or 問い合わせフォームへの導線）。
- **DESIGN.html 上の対応**: 設定タブ「学習」節の「リマインダー」行（値「毎日 21:00」＋シェブロン）／「サポート」節の「お問い合わせ」（`mail`アイコン）・「利用規約・プライバシー」（`description`アイコン）行。
- **依存するもの**: ローカル通知権限・スケジューリング（リマインダー）、問い合わせ手段・規約ページURLの確定（サポート）。

## 9. 効果音の実再生

- **何を作るか**: 正解時（短い上昇音）・クイズ完了時（小さなファンファーレ）の効果音を実際に鳴らす。サウンドON/OFFのトグル自体（`audioEnabledProvider`）は実装済み前提で、それに応じて再生有無を切り替える。
- **DESIGN.html 上の対応**: 明示的なモック要素はないが、正誤フィードバック（`ResultBanner`）・完了リング（`CompletionRing`）のハプティクスと対になる音響演出として設計方針に記載。**不正解には音・振動とも付けない方針**（responsesを責めないため）を維持する。
- **依存するもの**: 効果音素材（短い上昇音・ファンファーレ）、音声再生レイヤー（audioplayers 等、既存のナレーション再生と共存できる形）。

## 10. ipa_sg ブランド用 primary スワッチ

- **何を作るか**: `AppPrimarySwatch` のティール系 ramp（p50/p100/p300/p500/p600/p800 等）を新規に作り、ipa_sg の `AppConfig.brandPrimary` で差し替える。
- **DESIGN.html 上の対応**: 直接のモックはないが、デザインシステムの「RULE 3」（ブランド差し替えは `AppPrimarySwatch` という単位のみで行う）に基づく対応。既定は `focusBlue`。
- **依存するもの**: `engine/lib/src/design/app_primary.dart` の `AppPrimarySwatch` 定義（実装済み前提）。**RULE 3 に従い、差し替えは primary 系スワッチのみに留め、semantic固定色（correct/incorrect/review等）やaccentPinkには手を入れないこと**。

## 11. `videos/{id}.json` 詳細ファイル分離

- **何を作るか**: 現状 `base.json` に index-only で埋め込んでいる動画・章・クイズ情報を、動画単位の詳細JSON（`contents/videos/{id}.json`）に分離する。
- **DESIGN.html 上の対応**: 直接のモック要素はない（データ構造上の技術的検討事項）。動画視聴ページの確認クイズが肥大化した場合に備えた分割案。
- **依存するもの**: `content_models.dart` の `VideoChapter`/`VideoItem` 構造変更、`content_providers.dart` のロード処理変更。現状はクイズが小さいため `base.json` に同居させている。

---

関連: [docs/DESIGN.html](DESIGN.html)（デザイン原本）、[docs/LESSON.md](LESSON.md)（旧レッスンJSON仕様）
